-Xss2M
# Adding this flag temporarily to handle stack overflow
# which occurs on running `sbt fastOptJS`
# with default stack settings. This error did not occur
# before the PR which serializes the execution result